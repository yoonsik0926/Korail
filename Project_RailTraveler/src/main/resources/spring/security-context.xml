<?xml version="1.0" encoding="UTF-8"?>  
<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="
		http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context
		http://www.springframework.org/schema/context/spring-context.xsd
		http://www.springframework.org/schema/security
		http://www.springframework.org/schema/security/spring-security.xsd">

    <context:component-scan base-package="com.railer.rt"/>
	
    <!-- 권한이 없는 페이지를 접근할 경우 접근 불가 메시지 출력  -->
	<http auto-config="true">
		<!-- spring 4.x때 추가된 옵션으로 ssl을 사용하지 않을 경우 csrf는 disalbed=true로 설정. -->
		<csrf disabled="true"/>
		
		<!-- 스마트에디터 -->
		<headers>
			<frame-options policy="SAMEORIGIN"/>
			<hsts disabled="true"/>
		</headers>
		
		<intercept-url pattern="/" access="permitAll"/>
		<intercept-url pattern="/index.jsp" access="permitAll"/>
		<intercept-url pattern="/main" access="permitAll"/>
		
		<intercept-url pattern="/naeillo/**" access="permitAll"/>
		<intercept-url pattern="/station/**" access="permitAll"/>
		
		<intercept-url pattern="/tour/**" access="permitAll"/>
		
		<intercept-url pattern="/board/**" access="permitAll"/>
		<intercept-url pattern="/notice/**" access="permitAll"/>
		<intercept-url pattern="/qna/**" access="permitAll"/>
		<intercept-url pattern="/friend/**" access="permitAll"/>
		
		<intercept-url pattern="/plan/**" access="permitAll"/>
		<intercept-url pattern="/friendPlan/**" access="permitAll"/>
		
		<intercept-url pattern="/event/**" access="permitAll"/>
		
		<intercept-url pattern="/bookmark/**" access="permitAll"/>
		<intercept-url pattern="/ticket/**" access="permitAll"/>
		<intercept-url pattern="/stamp/**" access="permitAll"/>
		<intercept-url pattern="/bbs/**" access="permitAll"/>
		
		
		<intercept-url pattern="/resource/**" access="permitAll"/>
		<intercept-url pattern="/uploads/**" access="permitAll"/>
		
		<intercept-url pattern="/admin/**" access="hasRole('ROLE_ADMIN')"/>
		<intercept-url pattern="/singo/singo" access="hasRole('ROLE_ADMIN')"/>
		<intercept-url pattern="/member/totalList" access="hasRole('ROLE_ADMIN')"/>
		
		<intercept-url pattern="/member/*" access="permitAll"/>
		<intercept-url pattern="/mail/**" access="permitAll"/>
		
		
		<intercept-url pattern="/**" access="hasAnyRole('ROLE_USER','ROLE_ADMIN')"/>
		
		<form-login
			login-page="/member/login"
			login-processing-url="/member/login"
			username-parameter="userId"
			password-parameter="userPwd"
			
			default-target-url="/"
			always-use-default-target="false"
			authentication-success-handler-ref="loginSuccessHandler"
			authentication-failure-handler-ref="loginFailureHandler"
		/>
		
		<logout
			logout-url="/member/logout"
			invalidate-session="true"
			logout-success-url="/"
		/>
		
		<access-denied-handler error-page="/member/noAuthorized"/>
			
		<session-management>
			<concurrency-control max-sessions="1" expired-url="/member/expired"/>
		</session-management>
		
		<!-- AJAX를 위한 필터 등록 -->
		<custom-filter ref="ajaxFilter" after="EXCEPTION_TRANSLATION_FILTER"/>
		
	</http>
	
	<beans:bean id="myAuthenticationSuccessHandler" class="com.railer.rt.security.LoginSuccessHandler"/>
	
	<beans:bean id="loginSuccessHandler" class="com.railer.rt.security.LoginSuccessHandler">
    	<beans:property name="defaultUrl" value="/"/>
    </beans:bean>
               
    <beans:bean id="loginFailureHandler" class="com.railer.rt.security.LoginFailureHandler">
    	<beans:property name="defaultFailureUrl" value="/member/login?login_error"/> 
    </beans:bean>
	
	
	<jdbc-user-service data-source-ref="dataSource"
		id="userService"
		users-by-username-query="SELECT userId username, userPwd password, 
(case when enabled=1 then 1
else 0 end) enabled 
        FROM member2
        WHERE userId=?"
		authorities-by-username-query="SELECT userId username, authority FROM memberAuthority WHERE userId=?"
	/>
    
	<authentication-manager>
		<authentication-provider user-service-ref="userService">
			<password-encoder ref="bcryptEncoder"></password-encoder>
		</authentication-provider>
	</authentication-manager>
	
	<beans:bean id="bcryptEncoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder"/>

	<!-- AJAX 작업을 위한 필터 객체 -->
	<beans:bean id="ajaxFilter" class="com.railer.rt.security.AjaxSessionTimeoutFilter">
		<beans:property name="ajaxHeader" value="AJAX"/>
	</beans:bean>
</beans:beans>
